name=lvComponent
description=LV Test Component

parameter.xPos.name=X-Position
parameter.xPos.description=X Coordinate

parameter.yPos.name=Y-Position
parameter.yPos.description=Y Coordinate

parameter.xSize.name=Length
parameter.xSize.description=Length to add to X Position

parameter.ySize.name=Height
parameter.ySize.description=Height to add to Y Position

parameter.color.name=Color
parameter.color.description=Highlighting color

# parameter.location.name=Location
# parameter.location.description=Location for which to display a map. May be a country, city, full address, or anything the Google Geocoding API supports (https://developers.google.com/maps/documentation/geocoding/intro).
# parameter.pin.name=Pin
# parameter.pin.description=One or more variables that are updated when a user adds a pin to the map. The value to be saved will be a dictionary representing the pin, including fields for latitude and longitude. Use a!save() to save a modified or alternative value to a variable.
# parameter.key.name=Google API Key
# parameter.key.description=Key used to activate the Google Maps JavaScript API
